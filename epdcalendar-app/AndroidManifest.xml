<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hi.epdcalendar">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <!-- 自愈入口访问门槛：仅平台签名的锁屏引擎（system 分区应用）可拉起，防第三方滥用 -->
    <permission
        android:name="com.hi.epdcalendar.permission.HEAL"
        android:protectionLevel="signatureOrSystem"
        android:label="EpdCalendar heal"/>

    <!-- 合并版：控制台 App + Xposed 模块一体（模块部分见 assets/xposed_init →
         com.hi.epdclock.EpdClockModule，hook 装载进锁屏引擎与 system_server） -->
    <application
        android:label="墨水日历控制台"
        android:allowBackup="false"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <meta-data android:name="xposedmodule" android:value="true" />
        <meta-data android:name="xposeddescription" android:value="Hisense A2 Pro e-ink landscape calendar: wallpaper + live clock/battery overlay, hide OEM widgets" />
        <meta-data android:name="xposedminversion" android:value="54" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- 无感渲染宿主：透明窗口承载 WebView，结束即退，不出现在最近任务 -->
        <activity
            android:name=".RenderActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:excludeFromRecents="true"
            android:noHistory="true"/>

        <service android:name=".RefreshService" android:exported="false"/>

        <receiver android:name=".AlarmReceiver" android:exported="false"/>

        <!-- 看门狗拉起的自愈入口：透明宿主，重新布防+触发刷新后即退 -->
        <activity
            android:name=".HealActivity"
            android:exported="true"
            android:permission="com.hi.epdcalendar.permission.HEAL"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:excludeFromRecents="true"
            android:noHistory="true"/>

        <!-- 闹钟失联补救点：开机（闹钟全丢）/ 时间与时区跳变（RTC 锚点漂移）/
             应用更新（进程被杀）/ 每次解锁。布防幂等，重复触发无害 -->
        <receiver android:name=".BootReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.TIME_SET"/>
                <action android:name="android.intent.action.TIMEZONE_CHANGED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.USER_PRESENT"/>
            </intent-filter>
        </receiver>

        <!-- adb 调试入口：am broadcast 可远程配置/触发刷新 -->
        <receiver android:name=".ControlReceiver" android:exported="true">
            <intent-filter>
                <action android:name="com.hi.epdcalendar.ACTION_CONFIG"/>
                <action android:name="com.hi.epdcalendar.ACTION_REFRESH"/>
            </intent-filter>
        </receiver>
    </application>
</manifest>
